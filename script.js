// const inputValue = document.querySelector("#value");
// const divContainer = document.getElementsByTagName("h1")[0];
// console.log(divContainer);

// function convertEarnings(total) {
//   const result = (total / 100) * 32;
//   divContainer.innerText = `${result} баксов`;
//   inputValue.value = null;
//   return result;
// }

// const inputValue = document.querySelector("#value");
// const divContainer = document.getElementsByTagName("h1")[0];
// function handleFruitChange(total) {
//   const selectElement = document.getElementById("hours");
//   const selectedHours = +selectElement.value;
//   if (selectedHours === 90) {
//     const result = (total / 100) * 5;
//     divContainer.innerText = `${result} баксов`;
//     inputValue.value = null;
//     return result;
//   } else {
//     const result = (total / 100) * 10;
//     divContainer.innerText = `${result} баксов`;
//     inputValue.value = null;
//     return result;
//   }
// }
